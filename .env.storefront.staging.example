# Staging / preview smoke — copy to .env.storefront.staging.local (gitignored)
# Used by: npm run storefront:staging-qa

# Known preview host (ASCII only — do not paste Cyrillic in zsh exports)
STOREFRONT_SMOKE_BASE_URL=https://xn---preview--staging-r4nxb5ja9d6q.vercel.app
STOREFRONT_SMOKE_SLUG=hello
STOREFRONT_STRIPE_OPTION=A

# Week 1 redirect smoke (paths are storefront-relative suffixes)
# STOREFRONT_REDIRECT_FROM=/legacy-menu
# STOREFRONT_REDIRECT_TO=/menu
