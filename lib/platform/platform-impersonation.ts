/** Constants for impersonation cookie + session (see `ImpersonationSession` model). */
export const PLATFORM_IMPERSONATION_COOKIE = "kos_imp_session";

export const PLATFORM_IMPERSONATION_MAX_SECONDS = 60 * 60;
