/** Visual preset id stored in `StorefrontSettings.themePreset` (string). */
export type ThemePresetId =
  | "modern_minimal"
  | "warm_bakery"
  | "premium_catering"
  | "fresh_meal_prep"
  | "cafe_classic"
  | "ghost_kitchen_dark"
  | "organic_market"
  | "bold_restaurant"
  | string;

export type LayoutPresetId = "default" | "magazine" | "compact" | string;
