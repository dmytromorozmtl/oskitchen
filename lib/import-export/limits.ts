/** Hard caps for MVP import preview — tune per plan tier later. */
export const MAX_IMPORT_BYTES = 8 * 1024 * 1024;
export const MAX_IMPORT_ROWS = 10_000;
export const MAX_PREVIEW_ROWS_PERSISTED = 800;
