/** Re-exports storefront SEO helpers (canonical, metadata, JSON-LD). Prefer importing from here for storefront work. */
export {
  buildLocalBusinessJsonLd,
  buildMenuProductJsonLd,
  buildStorefrontMetadata,
  storefrontCanonicalBase,
} from "@/lib/storefront/seo";
