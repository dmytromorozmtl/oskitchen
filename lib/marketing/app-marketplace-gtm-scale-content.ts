/**
 * Absolute Final Task 134 — app marketplace GTM scale copy (feature 89).
 *
 * @see components/marketing/app-marketplace-third-party.tsx
 */

export const APP_MARKETPLACE_GTM_ICP_SUMMARY =
  "Multi-location operators needing certified SI connectors (Slack, Klaviyo, payroll) without building custom OAuth — disqualify if they expect Toast-style self-serve app store today." as const;

export const APP_MARKETPLACE_GTM_DEMO_HOOK =
  "Walk /app-marketplace catalog → Integrations → Extensions live state → Developers OAuth sandbox — label BETA/ROADMAP per row, never say “live app store.”" as const;

export const APP_MARKETPLACE_GTM_OBJECTION_REPLY =
  "We are BETA with platform review — not a self-serve public store. Certified SI partners scope implementation; OAuth apps queue for security sign-off." as const;

export const APP_MARKETPLACE_GTM_PRICING_TRACK =
  "70/30 revenue share on net app revenue — developers keep 70%. SI-led installs quoted separately; no bundled “marketplace included” in list pricing." as const;

export const APP_MARKETPLACE_GTM_PRIMARY_CTA =
  "Browse Integrations → Extensions in dashboard or register OAuth app at /developers — sales-safe: Illustrative catalog until publish pipeline exits review." as const;

export const APP_MARKETPLACE_GTM_HONESTY_SUMMARY =
  "Do not claim Toast/Square parity app store, self-serve install for all extensions, or LIVE badges on ROADMAP items. Always label BETA, ROADMAP, platform review, and Illustrative catalog — sales-safe talk tracks only." as const;
