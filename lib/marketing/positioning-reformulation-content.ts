import {
  POSITIONING_REFORMULATION_PRIMARY_LINE,
  POSITIONING_REFORMULATION_SUPPORTING_PILLARS,
} from "@/lib/marketing/positioning-reformulation-policy";

export const POSITIONING_REFORMULATION_EYEBROW =
  "Restaurant OS for the middle market" as const;

export const POSITIONING_REFORMULATION_SUBLINE =
  "Square stops at the counter. Toast locks you into their terminal. OS Kitchen is the full operating layer — on your hardware, with honest integration status." as const;

export const POSITIONING_REFORMULATION_ICP =
  "Meal prep, ghost kitchens, commissaries, and multi-concept operators outgrowing spreadsheets but not ready for a Toast hardware bundle." as const;

export { POSITIONING_REFORMULATION_PRIMARY_LINE, POSITIONING_REFORMULATION_SUPPORTING_PILLARS };
