name: Commercial Gate Execution Check

on:
  push:
    branches: [main]
    paths:
      - "lib/ops/commercial-gate-execution-*.ts"
      - "scripts/ops/run-commercial-gate-execution.ts"
      - "artifacts/commercial-gate-execution-summary.json"
  pull_request:
    paths:
      - "lib/ops/commercial-gate-execution-*.ts"
      - "scripts/ops/run-commercial-gate-execution.ts"
  workflow_dispatch:

jobs:
  commercial-gate-execution:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - name: Assess commercial gate execution (honest blocked state in CI)
        run: npm run ops:run-commercial-gate-execution -- --write || true
      - name: Upload execution report artifacts
        uses: actions/upload-artifact@v4
        with:
          name: commercial-gate-execution-report
          path: |
            artifacts/commercial-gate-execution-summary.json
            artifacts/commercial-gate-execution-report.html
          if-no-files-found: warn
