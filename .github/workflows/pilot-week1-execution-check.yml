name: Pilot Week 1 Execution Check

on:
  push:
    branches: [main]
    paths:
      - "lib/ops/pilot-week1-execution-*.ts"
      - "scripts/ops/run-pilot-week1-execution.ts"
      - "artifacts/pilot-week1-execution-summary.json"
  pull_request:
    paths:
      - "lib/ops/pilot-week1-execution-*.ts"
      - "scripts/ops/run-pilot-week1-execution.ts"
  workflow_dispatch:

jobs:
  pilot-week1-execution:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - name: Assess Pilot Week 1 execution (honest blocked state in CI)
        run: npm run ops:run-pilot-week1-execution -- --write || true
      - name: Upload execution report artifacts
        uses: actions/upload-artifact@v4
        with:
          name: pilot-week1-execution-report
          path: |
            artifacts/pilot-week1-execution-summary.json
            artifacts/pilot-week1-execution-report.html
          if-no-files-found: warn
