{
  "version": 1,
  "updatedAt": "2026-05-31",
  "apps": [
    {
      "clientId": "sandbox-opsbridge-connector",
      "name": "OpsBridge Connector",
      "publisher": "KitchenOS Certified SI — OpsBridge",
      "status": "SANDBOX",
      "description": "Sandbox OAuth app for Slack ops alerts and order event fan-out. For pilot partners only.",
      "redirectUris": [
        "http://localhost:3000/api/oauth/callback/sandbox",
        "https://hooks.example.com/oauth/kitchenos/callback"
      ],
      "allowedScopes": ["read:orders", "manage:webhooks"],
      "embedUrl": "/developers/embed-demo",
      "embedOrigins": ["http://localhost:3000", "https://kitchenos.com"],
      "honestyNote": "Sandbox — not a production app store listing. Requires integrations.manage to install."
    },
    {
      "clientId": "sandbox-growthkitchen-sync",
      "name": "GrowthKitchen CRM Sync",
      "publisher": "KitchenOS Certified SI — GrowthKitchen",
      "status": "SANDBOX",
      "description": "Sandbox OAuth app for order + customer read paths into Klaviyo-style journeys.",
      "redirectUris": [
        "http://localhost:3000/api/oauth/callback/sandbox",
        "https://partners.example.com/oauth/callback"
      ],
      "allowedScopes": ["read:orders", "read:products", "read:inventory"],
      "honestyNote": "Sandbox — customer PII remains minimized on API responses."
    },
    {
      "clientId": "capital-flexcap-rbf-us",
      "name": "FlexCap Capital OAuth",
      "publisher": "FlexCap — KitchenOS capital partner",
      "status": "PUBLISHED",
      "description": "OAuth client for FlexCap to pull signed KitchenOS revenue attestations after merchant consent.",
      "redirectUris": [
        "https://apply.flexcap.example/oauth/kitchenos/callback",
        "http://localhost:3000/api/oauth/callback/sandbox"
      ],
      "allowedScopes": ["read:capital_attestation", "read:capital_referrals"],
      "honestyNote": "Capital partner — attestation pull only; KitchenOS does not originate loans."
    },
    {
      "clientId": "capital-pilot-rbf-partner",
      "name": "Pilot RBF Capital OAuth (sandbox)",
      "publisher": "KitchenOS pilot lender sandbox",
      "status": "SANDBOX",
      "description": "Sandbox OAuth client for pilot lender attestation pull during Restaurant Capital Phase 6.",
      "redirectUris": [
        "https://partners.example.com/oauth/kitchenos/callback",
        "http://localhost:3000/api/oauth/callback/sandbox"
      ],
      "allowedScopes": ["read:capital_attestation", "read:capital_referrals"],
      "honestyNote": "Sandbox capital OAuth — not a live funding guarantee."
    }
  ]
}
