const fs = require("node:fs");
const path = require("node:path");

const target = path.join(__dirname, "..", "node_modules", "object-inspect", "util.inspect.js");
const contents = `// Auto-generated by scripts/ensure-object-inspect-shim.cjs — do not edit by hand.
module.exports = require("util").inspect;
`;

try {
  fs.mkdirSync(path.dirname(target), { recursive: true });
  fs.writeFileSync(target, contents, "utf8");
} catch {
  // install may be incomplete in partial CI — ignore
}
