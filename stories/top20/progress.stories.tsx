import { Progress } from "@/components/ui/progress";
import { top20StoryMeta } from "./_story-meta";

const meta = top20StoryMeta("Progress", Progress);
export default meta;

export const SetupWizard = { args: { value: 65, className: "w-[280px]" } };
