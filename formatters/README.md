# Formatters

Currency, dates, phone numbers, and label strings belong here — keep UI components free of locale math.

Reuse `lib/utils` helpers where they already exist; extract when duplication appears across dashboards.
