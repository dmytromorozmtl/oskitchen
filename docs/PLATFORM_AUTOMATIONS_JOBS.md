# Platform automations & jobs

## Automations (`/platform/automations`)

- Lists recent `AutomationExecution` rows with rule name and status.
- Retry/disable flows not yet wired — use `platform:automations:repair` when adding actions.

## Jobs / queues (`/platform/jobs`)

- Stub — connect worker metrics and stuck job queries later.

## Notifications (`/platform/notifications`)

- Stub — fan-out health and template debugging.
