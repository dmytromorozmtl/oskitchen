# Forecast QA checklist

- [x] `/dashboard/forecast` renders without an active menu for Restaurant / Café / Bar / Catering / Bakery / Ghost Kitchen modes.
- [x] Meal Prep mode shows a softer "Set an active weekly menu" hint that doesn't block the page.
- [x] Empty state appears when there are no orders, menus, meal plans, or catering events.
- [x] Wizard supports all forecast types (`PRODUCT_DEMAND` is the default).
- [x] Wizard supports all sources; defaults follow business mode.
- [x] Running a forecast persists a `forecast_runs` row, builds `forecast_lines`, emits `RUN_CREATED` + `RUN_COMPLETED` events.
- [x] Run detail shows product, catering, and ingredient lines when applicable.
- [x] Manual adjustment (PERCENT / FIXED_QUANTITY / OVERRIDE) updates forecast / buffer / recommended quantities and audits the change.
- [x] OVERRIDE flips the line confidence to MANUAL.
- [x] "Send to production" creates a draft `ProductionBatch` and emits `SENT_TO_PRODUCTION`.
- [x] "Send to ingredient demand" creates a draft `IngredientDemandRun` and emits `SENT_TO_INGREDIENT_DEMAND`.
- [x] Archive / restore toggles `status` between `COMPLETED` and `ARCHIVED`.
- [x] History page lists runs newest first.
- [x] Settings page shows business-mode default buffer and default sources.
- [x] No forecast logic invokes external APIs.
- [x] Forecast labels output as "Estimate only" everywhere.
- [x] Permission gate honours `workspace.moroz@gmail.com` superadmin override.
- [x] `npm run typecheck` passes.
- [x] `npm run build` passes.
