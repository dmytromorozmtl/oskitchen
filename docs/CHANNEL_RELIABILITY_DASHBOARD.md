# Channel reliability dashboard

`/dashboard/sales-channels/reliability` shows a **7-day** tenant snapshot:
- Webhook processed success vs error vs pending counts.
- Sync job SUCCESS vs FAILED counts.
- Import batches in `NEEDS_REVIEW` and open conflicts.

Partner SLA + downtime cards remain placeholders until telemetry contracts exist.
