# Demand by supplier

`groupDemandRowsBySupplier` groups `IngredientDemandRow` by `supplier` string (fallback **Unassigned supplier**).

The **By supplier** tab mirrors the purchasing assistant style lists (first eight lines per supplier + CSV for full fidelity).

Future: join vendor master data, MOQs, lead times once a `Supplier` model exists.
