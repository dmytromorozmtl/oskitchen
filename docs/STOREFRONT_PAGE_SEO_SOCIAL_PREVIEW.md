# Per-page SEO & Social Preview

## Сейчас

- `StorefrontPage.seoTitle`, `seoDescription`, slug.
- Глобальные SEO поля в `StorefrontSettings` (см. SEO tab).

## Цель

- OG image per page (новое поле или `contentJson.meta.ogImage`).
- Превью карточки Google / Twitter в админке (read-only шаблон).

## Риск

- Дублирование canonical — валидатор относительно `canonicalBaseUrl`.
