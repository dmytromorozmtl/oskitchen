# Marketing Positioning & Website (Final)

## Core headline / subheadline (homepage)

Implemented in `components/landing/hero.tsx`:

- **Headline:** “The operating system for modern food operations.”  
- **Eyebrow:** “Commerce OS + Operations OS”  
- **Subhead:** “Run POS sales, online orders, production, packing, delivery, customers, and integrations from one workspace.” + honesty clause on third-party live claims.

## Integration language in marketing

Use the same tiers as `docs/INTEGRATION_MATURITY_MATRIX.md` — **never** “Uber Eats live” unless the workspace is partner-approved and operationally verified.

## Segment pages

`/solutions/*` and `/product/*` should echo **one operational story** each: intake → kitchen → fulfillment → money.

## CTA map

`/signup`, `/demo`, `/book-demo`, `/beta`, `/contact-sales` — monitor 404s in CI build output.
