# Copy to repo root as .env.upstash.paste.local (gitignored):
#   cp docs/templates/UPSTASH_PASTE.env.example .env.upstash.paste.local
#
# 1. https://console.upstash.com/redis → your DB → REST API
# 2. Paste real values below (not doc placeholders)
# 3. npm run pilot:upstash:gate

UPSTASH_REDIS_REST_URL=https://us1-example-12345.upstash.io
UPSTASH_REDIS_REST_TOKEN=
