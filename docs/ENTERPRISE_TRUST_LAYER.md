# Enterprise trust layer

## Already present

- Audit logs UI (`/dashboard/audit-logs/*`), RBAC primitives for dashboard + platform, data export hooks, retention policy surfaces.
- Credential handling via integration connections (never render raw tokens in client components).

## Honest roadmap items (do not claim completed)

- SSO / SCIM — roadmap only unless implemented end-to-end.
- DPA template — legal artifact, not code.
- Public uptime page — placeholder acceptable if labeled “status roadmap”.

## Priority

**P1** disclosures; **P0** for any customer-facing security regression.
