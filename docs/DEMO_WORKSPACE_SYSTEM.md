# Demo workspace system

## Requirements

- Clearly labeled demo data (`isChannelTestOrder`, demo flags where present).
- **Reset** action only inside designated demo workspace templates (`WorkspaceTemplate` / seed scripts — verify before exposing destructive button).
- Never insert demo rows into production workspaces without explicit admin confirmation.

## Priority

**P2** sales enablement; **P1** if outbound demos are primary GTM.
