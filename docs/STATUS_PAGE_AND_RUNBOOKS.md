# Status page & runbooks

## Public / operator snapshot
- `/trust/status` — async server page using `loadExtendedHealthSnapshot` + component checklist (non-contractual).

## Platform
- `/platform/runbooks` — index of markdown paths under `docs/runbooks/` for internal staff.

## Runbook files
- See `docs/runbooks/*.md` (7 topics: webhooks, imports, email, POS, storefront, DB migrations, support escalation).

## Incident pages
- Existing `/platform/incidents` remains source for structured incidents when used.
