# Menu item import / export

## Today

- Implementation import job type `PRODUCTS` resolves target menu as: first **active service menu**, else **catalog menu** after `ensureCatalogMenu` (`actions/implementation.ts`).

## Next

- Dedicated `/dashboard/products/import` with CSV template download, validation preview, duplicate detection, and image URL column.
