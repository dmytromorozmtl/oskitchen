# Product mapping (final)

- **Workbench:** `/dashboard/product-mapping`
- **Hub entry:** `/dashboard/sales-channels/mapping` shows unmatched `ExternalProduct` counts + deep link
- **Model:** `ProductMapping` (suggestions, confidence, status)

Future: auto-queue suggestions when sync encounters unknown SKUs.
