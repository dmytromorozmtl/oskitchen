# Channel onboarding assistant

`/dashboard/sales-channels/assistant` ships deterministic guidance (no AI key required).

## Future
When an AI key exists, hydrate the same checklist with generated prose while **preserving** these safety rails: no fake live approvals, no secret echo.
