# Menu item detail editor (roadmap)

Target route: `/dashboard/products/[productId]` with tabs: Overview, Pricing & margin, Recipe, Availability, Menus, Storefront & channels, Production, Nutrition & labels, Reports, Settings.

**Current:** edit/delete via dialog on `/dashboard/products` (per menu tab).

**Next steps:** lift the existing `ProductForm` fields into tabbed sections; add readiness checklist (cost, allergens, nutrition, channel mapping); deep links from costing and nutrition surfaces.
