# P1-28 — Demo page interactive sandbox with test credentials + LIVE integrations

**Policy:** `demo-interactive-sandbox-p1-28-v1`  
**Registry:** [`artifacts/demo-interactive-sandbox-p1-28.json`](../artifacts/demo-interactive-sandbox-p1-28.json)

## Contract

`/demo` must offer an **interactive sandbox** (not video-only) with:

1. **Test credentials panel** — guest launch pattern, session duration, staging smoke env keys
2. **LIVE integration markers** — Shopify, WooCommerce, storefront, POS with smoke artifact refs
3. **Interactive Integration Health workspace** — clickable channels with PASS/SKIPPED/FAILED

Video tour (P1-83) remains secondary below the sandbox.

## Verify

```bash
npm run check:demo-interactive-sandbox-p1-28
```
