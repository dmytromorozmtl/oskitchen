# OS Kitchen — AUTONOMOUS LOOP EXECUTOR v5.0

Each invocation: read `artifacts/execution-log.txt` → diagnose → ONE fix → commit → append log.

**Invoke:** "Выполни один цикл по v5.0"

**Stop when:** `artifacts/final-execution-report.json` shows `ready: true` and `pilotExecutableScore: 100`.

Full prompt body: see chat / parent documentation.
