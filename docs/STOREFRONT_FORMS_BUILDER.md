# Storefront forms builder

**Status:** Not implemented in this pass beyond existing Prisma models (`StorefrontForm`, `StorefrontFormSubmission`).

## Planned MVP

- Admin CRUD for forms, field builder JSON, honeypot name per form.
- Public render on contact/catering pages when attached.
- Submissions stored; email notifications best-effort when provider configured.

See `docs/STOREFRONT_COMPLETION_REPORT.md` for scheduling.
