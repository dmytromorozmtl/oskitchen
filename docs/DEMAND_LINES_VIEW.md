# Demand lines view

Rendered in the **Demand lines** tab of `/dashboard/inventory/demand`.

Columns: prep date, ingredient (+ unit/supplier/conversion flag), required, stock, short, related products (first four titles).

CSV export mirrors the same numeric fields for Purchasing compatibility.
