# Menu production handoff

Production remains **product-driven** (`Product` → `ProductionTask`). Strategy definitions document intended grouping (by prepared date, by station, by event timeline) for UX and future automation.

Follow-up: auto-task rules keyed off `MenuStrategy` + `preparedDatesJson` without duplicating `Product`.
