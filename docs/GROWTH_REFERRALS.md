# Growth — Referrals

## Tables

- `referral_codes` — per-user shareable codes.
- `referral_events` — clicks / conversions (`convertedUserId`).

## Metrics in Command Center

`referralSummary()` exposes active codes, total events, attributed signups.

## Roadmap

Leaderboards, partner programs, ambassador tiers, campaign-specific codes.
