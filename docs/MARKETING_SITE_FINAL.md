# Public Marketing Website — Final (MVP)

## Positioning

**KitchenOS = Commerce OS + Operations OS** for modern food businesses.

## Homepage (implemented direction)

- **Hero:** `components/landing/hero.tsx` — headline + subhead per positioning; **honest** integration language (channels depend on customer configuration).  
- **CTAs:** Start free trial, Explore demo, Book demo, Join beta — routes must 200.

## Required pages (verify routes exist)

`/`, `/solutions`, `/solutions/*` segments, `/product`, `/product/*` modules, `/integrations`, `/pricing`, `/demo`, `/book-demo`, `/beta`, `/trust`, `/resources`, `/partners`, `/contact-sales`.

## Trust & compliance copy

- No fake certifications.  
- No “live Uber Eats / DoorDash” unless describing **customer-owned** connections.  
- Stripe / Terminal: describe as **roadmap** or **optional** unless code + env prove otherwise.

## P2

- Replace long prose blocks with product screenshots + short captions.  
- SEO metadata per segment page.
